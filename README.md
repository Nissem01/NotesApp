A console based app for a school assignment. A user can create/login to an account where they then have the option to create notes that gets stored in a database.
Dependencies:
jbcrypt-0.4.jar,
mysql-connector-j-9.6.0.jar
